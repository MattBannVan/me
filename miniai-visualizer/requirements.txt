# MiniAI Visualizer — Windows / NVIDIA RTX 5060 (Blackwell)
#
# IMPORTANT: install PyTorch FIRST with the CUDA 12.8 index (see README.md):
#   pip install "torch>=2.7" --index-url https://download.pytorch.org/whl/cu128
#
# The RTX 5060 is a Blackwell GPU (compute capability sm_120); wheels older
# than PyTorch 2.7 / cu128 do NOT include sm_120 kernels and will fall back
# to CPU or error out.

torch>=2.7
customtkinter>=5.2.2
safetensors>=0.4.3
numpy>=1.26
